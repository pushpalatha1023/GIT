git init
git status
git add or git add .
git commit -m "custom msg"
git remote add origin "repo link"
git push origin main/master
git clone "url of repo" --> to clone the repo
git pull "url of repo"
git branch <name of new branch>
git branch -D <branch name > -->to delete
git checkout <branch name>
git log
git stash
git stash -u 
for retrieveing : git stash pop
git revert <commit id>
git diff <commit-id version x> <commit-is version y>
